# CPU-only PyTorch build. Left unpinned, plain "torch" on PyPI resolves to
# the CUDA/GPU build, pulling several GB of NVIDIA driver libraries
# (nvidia-cublas, nvidia-cudnn, nvidia-nccl, etc.) that are useless on
# Streamlit Community Cloud's CPU-only free tier and were very likely
# what pushed the app past its memory limit and crashed it.
# 2.13.0+cpu is the version confirmed available for this environment's
# Python (3.14) on the PyTorch CPU index.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit
langchain
langchain-community
langchain-core
langchain-text-splitters
langchain-huggingface
langchain-chroma
chromadb
sentence-transformers
transformers
pypdf
torch==2.13.0+cpu
python-dotenv
tiktoken
requests
numpy
pymupdf
fpdf2
